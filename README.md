# 3DmodelsProject
Project used to inject the QR of a 3D model into a webpage based on the parameters in the URL.

This version of the project is diferent from the one created previously. This one doesnt redirect the user to the viewer.html or default.html pages. It instead it serves the page while keeping the URL intact. Which is important.
